BankingController

          import java.time.Period;
          /*
            Period period = diferenciaEntreFechas.calculateYearsOfIndependency(product.get().getIssueDate());  
            productResponse.setDayssOfIndependency(period.getDays());
            productResponse.setMonthsOfIndependency(period.getMonths());
            productResponse.setYearsOfIndependency(period.getYears());
            */
productResponse

    /* 
    public int yearsOfIndependency;
    public int monthsOfIndependency;
    public int dayssOfIndependency;
    */

        /* 
    public void setYearsOfIndependency(int yearsOfIndependency) {
        this.yearsOfIndependency = yearsOfIndependency;
    }
    public void setMonthsOfIndependency(int monthsOfIndependency) {
        this.monthsOfIndependency = monthsOfIndependency;
    }
    public void setDayssOfIndependency(int dayssOfIndependency) {
        this.dayssOfIndependency = dayssOfIndependency;
    }
    */

Test Ping
@Mock
    private Ping pingService;

    @InjectMocks
    private BankingController pingController;

    public BankingControllerTest() {
        MockitoAnnotations.initMocks(pingController); // Inicializar mocks
    }

    @Test
    void testPingResponse() throws IOException{

         // Arrange
        String host = "google.com";
        String expectedResponse = "Sent Ping Request to google.com: Host is reachable";
        when(pingService.sendPingRequest(host)).thenReturn(expectedResponse);

        // Act
        String actualResponse = pingController.pingResponse(host);

        // Assert
        assertEquals(expectedResponse, actualResponse);
        verify(pingService).sendPingRequest(host);
        
    }